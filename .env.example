GEMINI_API_KEY=
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_CHAT_ID=

# Optional. Comma-separated model(s) to fall back to when the primary model
# (JARVIS_MODEL, or its default) hits a Gemini free-tier rate limit -- daily
# cap or per-minute burst. Defaults to gemini-3.6-flash if unset.
# JARVIS_FALLBACK_MODELS=gemini-3.6-flash
